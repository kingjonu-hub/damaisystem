[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node src/database/start.js && node src/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
